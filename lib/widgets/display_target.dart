import 'package:flutter/material.dart';

class DisplayTargetWidget extends StatelessWidget {
  const DisplayTargetWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
