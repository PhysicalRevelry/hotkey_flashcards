import 'package:flutter/material.dart';

class HotkeyFlashCards extends StatelessWidget {
  const HotkeyFlashCards({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
